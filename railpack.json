{
  "steps": {
    "composer-update": {
      "inputs": [
        { "step": "build" }
      ],
      "commands": [
        "composer update --no-interaction --optimize-autoloader"
      ]
    }
  }
}
